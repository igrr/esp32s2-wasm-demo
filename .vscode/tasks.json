{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (emmake)",
            "type": "shell",
            "command": "source ${EMSDK_PATH}/emsdk_env.sh 2>/dev/null && emmake make all",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Clean (emmake)",
            "type": "shell",
            "command": "source ${EMSDK_PATH}/emsdk_env.sh 2>/dev/null && emmake make clean",
            "problemMatcher": []
        }
    ]
}